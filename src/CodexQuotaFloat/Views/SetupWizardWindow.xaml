<Window x:Class="CodexQuotaFloat.Views.SetupWizardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="配置 Codex CLI" Width="520" SizeToContent="Height" MinHeight="300"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize" Background="#171B22" Foreground="#F4F7FB">
  <Grid Margin="28">
    <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
    <TextBlock Text="{Binding Title}" FontSize="22" FontWeight="SemiBold" Margin="0,0,0,16"/>
    <TextBlock Grid.Row="1" Text="{Binding Message}" TextWrapping="Wrap" Foreground="#D5DCE6" LineHeight="22"/>
    <StackPanel Grid.Row="2" Margin="0,22,0,0">
      <TextBlock Text="官方 Windows 安装命令：" Visibility="{Binding ShowInstallActions, Converter={StaticResource BooleanToVisibilityConverter}}" Foreground="#AEB9C7" Margin="0,0,0,6"/>
      <TextBox Text="powershell -ExecutionPolicy ByPass -c &quot;irm https://chatgpt.com/codex/install.ps1 | iex&quot;" IsReadOnly="True" TextWrapping="Wrap" Visibility="{Binding ShowInstallActions, Converter={StaticResource BooleanToVisibilityConverter}}" Background="#101318" Foreground="#E8EDF5" BorderBrush="#3AFFFFFF" Padding="8"/>
      <TextBlock Text="备用 npm 命令：npm install -g @openai/codex" Visibility="{Binding ShowInstallActions, Converter={StaticResource BooleanToVisibilityConverter}}" Foreground="#AEB9C7" Margin="0,8,0,0"/>
      <TextBlock Text="{Binding Result.CliPath, StringFormat=CLI 路径：{0}}" Visibility="{Binding ShowDiagnostics, Converter={StaticResource BooleanToVisibilityConverter}}" Foreground="#AEB9C7" Margin="0,8,0,0"/>
      <TextBlock Text="{Binding Result.CliVersion, StringFormat=CLI 版本：{0}}" Visibility="{Binding ShowDiagnostics, Converter={StaticResource BooleanToVisibilityConverter}}" Foreground="#AEB9C7" Margin="0,4,0,0"/>
    </StackPanel>
    <WrapPanel Grid.Row="3" Margin="0,26,0,0" HorizontalAlignment="Right">
      <Button Style="{StaticResource DarkActionButton}" Content="复制官方安装命令" Command="{Binding CopyInstallCommandCommand}" Visibility="{Binding ShowInstallActions, Converter={StaticResource BooleanToVisibilityConverter}}" Margin="5"/>
      <Button Style="{StaticResource DarkActionButton}" Content="打开 PowerShell" Command="{Binding OpenPowerShellCommand}" Visibility="{Binding ShowInstallActions, Converter={StaticResource BooleanToVisibilityConverter}}" Margin="5"/>
      <Button Style="{StaticResource DarkActionButton}" Content="开始登录" Command="{Binding StartLoginCommand}" Visibility="{Binding ShowLoginAction, Converter={StaticResource BooleanToVisibilityConverter}}" Margin="5"/>
      <Button Style="{StaticResource DarkActionButton}" Content="打开日志目录" Command="{Binding OpenLogsCommand}" Visibility="{Binding ShowDiagnostics, Converter={StaticResource BooleanToVisibilityConverter}}" Margin="5"/>
      <Button Style="{StaticResource DarkActionButton}" Content="复制诊断信息" Command="{Binding CopyDiagnosticsCommand}" Visibility="{Binding ShowDiagnostics, Converter={StaticResource BooleanToVisibilityConverter}}" Margin="5"/>
      <Button Style="{StaticResource DarkActionButton}" Content="重新检测" Command="{Binding RetryCommand}" Margin="5"/>
      <Button Style="{StaticResource DarkActionButton}" Content="稍后设置" IsCancel="True" Click="LaterButton_Click" Margin="5"/>
    </WrapPanel>
  </Grid>
</Window>
